# Increase memory limit for Node.js
node_options=--max_old_space_size=4096

# Use legacy peer dependencies to avoid issues
legacy-peer-deps=true

# Disable strict SSL to prevent potential certificate issues
strict-ssl=false
